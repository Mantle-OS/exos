header:
  version: 14

local_conf_header:
  0010_mantle_default_ota__appends:
    PACKAGE_FEED_URIS ?= "https://repos.mantle-os"
    PACKAGE_TYPE ?= "rpm"
    PACKAGE_FEED_BASE_PATHS ?= "rpm"
    PACKAGE_CLASSES ?= "package_rpm"
    IMAGE_FEATURES:append = " package-management "
    IMAGE_INSTALL:append = " ${PACKAGE_TOOLS} "
